require 'spec_helper'

describe Eratosthenes do
  it 'has a version number' do
    expect(Eratosthenes::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
